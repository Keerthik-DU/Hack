/**
 * Synthetic secret samples for WO-050 / detection E2E — NOT real credentials.
 * Values are obviously fake (EXAMPLE / xxxxxxxx placeholders).
 */
export const config = {
  // AWS access key ID (official Amazon example key material)
  awsAccessKeyId: 'AKIAIOSFODNN7EXAMPLE',
  awsSecretAccessKey: 'wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY',

  // GitHub personal access token (fake — all x placeholders)
  githubToken: 'ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx',

  // Generic password assignment used by entropy/contextual heuristics
  password: 'SuperFakePassword_ForTestsOnly_12345',

  databaseUrl: 'postgres://demo_user:demo_pass@localhost:5432/airgap_demo',
};

export function printConfig(): void {
  console.log('Loading fake config for scanner validation only');
  console.log(config.awsAccessKeyId);
  console.log(config.githubToken);
}
