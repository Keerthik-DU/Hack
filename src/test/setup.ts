import { beforeAll, afterAll, beforeEach, afterEach } from 'vitest';

beforeAll(() => {
  // Global setup logic before all test suites
});

afterAll(() => {
  // Global cleanup logic after all test suites
});

beforeEach(() => {
  // Per-test setup logic
});

afterEach(() => {
  // Per-test cleanup logic
});
